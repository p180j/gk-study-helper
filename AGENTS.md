# AGENTS.md

## 1. 项目说明

本仓库为：

```text
gk-study-helper
```

产品定位：

> 个人考公 AI 学习助手，通过真实学习数据持续形成能力画像、发现学习问题，并自动决定下一步最值得学习的内容。

开发前必须优先阅读：

```text
docs/product-plan.md
docs/progress.md
```

`docs/product-plan.md` 是产品基线。

未经明确要求，不得擅自扩大产品范围。

---

# 2. 核心开发原则

开发必须始终围绕以下主闭环：

```text
题目 / 学习内容
↓
真实学习行为
↓
能力画像
↓
学习问题
↓
问题优先级
↓
每日计划
↓
继续训练
↓
验证问题是否改善
```

任何新增设计都应优先判断：

1. 是否帮助更准确地判断能力？
2. 是否帮助更准确地发现学习问题？
3. 是否帮助更合理地决定下一步学习？
4. 是否减少内容和题库维护成本？

如果都没有明显帮助，不应主动增加。

---

# 3. 开发阶段原则

本项目不采用大量小版本推进。

按照 `docs/product-plan.md` 中定义的完整阶段推进。

不要因为普通实现细节：

```text
新建一个“小版本”
新增一个 iteration
新增一个 migration 文件
重新讨论产品方向
```

只有涉及以下问题时才需要暂停并请求决策：

```text
会改变核心产品逻辑
会改变数据库核心模型
会造成历史数据不可逆
会影响整体架构
存在明显安全风险
```

普通技术实现问题自行选择：

> 简单、稳定、容易迁移、容易维护的方案。

---

# 4. SQL 规范

SQL 目录长期只维护：

```text
sql/
├─ schema.sql
├─ init_data.sql
└─ upgrade.sql
```

禁止主动创建：

```text
migration_v1.sql
migration_v2.sql
iteration_01.sql
patch_xxx.sql
fix_xxx.sql
```

规则：

```text
当前数据库增量
↓
先写 upgrade.sql
↓
实际执行验证
↓
同步合并到 schema.sql
↓
清理 upgrade.sql
```

`schema.sql` 必须始终代表：

> 当前版本从全新空数据库开始所需要的完整数据库结构。

---

# 5. 配置规范

配置文件尽量保持：

```text
application.yml
application-local.yml
application-prod.yml
```

禁止因为每个模块单独增加大量配置文件。

数据库密码、AI Key、微信 Secret 等敏感内容不得提交真实值。

使用环境变量，例如：

```text
DB_URL
DB_USERNAME
DB_PASSWORD
AI_API_KEY
WX_APP_ID
WX_APP_SECRET
```

业务型配置优先进入数据库，而不是不断增加 yml。

---

# 6. 架构原则

当前阶段：

```text
Spring Boot 单体应用
MySQL
微信小程序
管理后台
```

不得未经要求主动引入：

```text
微服务
服务注册中心
Gateway
RocketMQ
Kafka
分布式事务
复杂缓存集群
复杂DDD分层
```

Redis 仅在出现明确实际需求以后考虑。

---

# 7. 数据原则

真实学习数据优先级高于计算结果。

以下数据必须尽量保留：

```text
答题记录
用户答案
标准答案快照
答题耗时
训练场景
题目版本
知识点快照
难度快照
申论历史作答
学习问题历史
```

能力分可以重新计算。

原始学习行为不能因为算法调整而丢失。

---

# 8. 算法原则

AbilityEngine、LearningProblemEngine、ProblemPriorityEngine、DailyPlanEngine 应保持：

```text
集中
可解释
可测试
可回放
```

禁止把核心算法大量散落在：

```text
Controller
Mapper
页面代码
多个无关 Service
```

算法修改以后应尽量能够：

```text
读取历史真实数据
↓
重新计算
↓
对比新旧结果
```

---

# 9. AI 使用原则

AI 不作为能力评分的唯一事实来源。

原则：

```text
规则 + 数据
负责判断事实

AI
负责理解、解释、归纳和生成
```

AI 可以用于：

```text
错因分析
申论批改
政治阅读提炼
每日 / 每周复盘
题目标签
原创题
变式题
```

未经可靠校验的 AI 输出不能直接污染核心能力数据。

---

# 10. 内容自动化原则

目标：

> 正常内容尽量自动处理，人工只处理异常。

自动化可以负责：

```text
解析
拆题
去重
标签
难度判断
答案检查
库存检查
内容分类
```

低置信度内容进入异常处理。

不得为了“自动化”让低质量内容自动扩散。

---

# 11. 每次开发后的强制动作

完成一个实质性开发任务后，不能只输出：

> 已完成。

必须实际执行能够执行的验证。

根据本次修改范围至少执行：

```text
编译
自动测试
数据库验证
接口验证
运行验证
核心业务链验证
```

如果出现失败：

```text
先修复
↓
重新执行验证
```

不能在测试失败时把任务标记为完成。

---

# 12. 核心链路验证原则

当开发涉及以下模块：

```text
题库
答题
能力
学习问题
计划
```

必须尽可能真实验证：

```text
导入 / 创建题目
↓
完成答题
↓
产生 answer_record
↓
ability_profile 更新
↓
ability_history 产生记录
↓
learning_problem 产生或更新
↓
重新生成 daily_plan
↓
训练重点发生合理变化
```

如果对应阶段要求这条链路，而链路没有实际跑通：

> 不得标记阶段完成。

---

# 13. 空库验收

涉及数据库结构的重要阶段，必须至少验证一次：

```text
全新空数据库
↓
仅执行 schema.sql
↓
执行 init_data.sql
↓
启动应用
```

如果必须额外执行：

```text
patch
历史 migration
临时 SQL
手工改表
```

才能启动，则数据库基线未通过验收。

---

# 14. product-plan 偏离检查

每次完成一个大块开发后必须检查：

```text
docs/product-plan.md
```

重点确认是否出现：

```text
未规划功能扩张
无必要的架构复杂化
重复数据模型
过度拆包
大量SQL文件
大量配置文件
额外中间件
```

如果存在偏离，应：

1. 优先自行恢复到产品基线；
2. 如果偏离确有必要，在验收报告中明确说明原因；
3. 不得静默修改产品方向。

---

# 15. progress.md 更新规则

完成验收以后同步更新：

```text
docs/progress.md
```

只标记真正通过验证的项目。

禁止：

```text
代码刚生成
→ 直接打勾
```

必须：

```text
代码完成
+
实际验证
+
验收通过
→ 才能打勾
```

---

# 16. 验收报告

每次完成一个完整开发块后，必须覆盖更新：

```text
docs/acceptance-report.md
```

不要为每次开发创建新的验收报告文件。

`acceptance-report.md` 永远代表：

> 最近一次开发任务的最新验收情况。

---

# 17. acceptance-report.md 固定格式

必须按以下结构输出：

````markdown
# 开发验收报告

## 1. 本次任务

本次目标：

当前所属阶段：

对应 product-plan：

---

## 2. 实际完成

- 
- 
- 

---

## 3. 主要修改文件

### 新增

- 

### 修改

- 

### 删除

- 

---

## 4. 数据库变化

是否修改数据库：

YES / NO

修改内容：

- 

schema.sql 是否已同步：

YES / NO / N/A

init_data.sql 是否变化：

YES / NO

upgrade.sql 当前状态：

---

## 5. 接口变化

新增接口：

- 

修改接口：

- 

---

## 6. 编译与测试

### 编译

命令：

结果：

PASS / FAIL

### 自动测试

命令：

通过：

失败：

结果：

PASS / FAIL

---

## 7. 启动验证

Spring Boot：

PASS / FAIL / N/A

数据库连接：

PASS / FAIL / N/A

关键启动日志：

---

## 8. 实际业务验证

本次实际执行的业务场景：

1.
2.
3.

实际结果：

---

## 9. 核心链路验证

涉及核心学习闭环：

YES / NO

若 YES：

```text
题目
→ 答题
→ AnswerRecord
→ Ability
→ LearningProblem
→ DailyPlan
````

验证结果：

PASS / FAIL

具体数据：

* 答题记录：
* 能力变化：
* 学习问题变化：
* 计划变化：

---

## 10. 产品规划偏离检查

是否偏离 `docs/product-plan.md`：

NO / YES

若 YES：

偏离内容：

原因：

是否需要用户决策：

YES / NO

---

## 11. 架构复杂度检查

是否新增：

* 微服务：NO / YES
* Redis：NO / YES
* MQ：NO / YES
* 新SQL文件：NO / YES
* 新配置文件：NO / YES
* 其他复杂基础设施：NO / YES

说明：

---

## 12. 本次发现并修复的问题

1.
2.

---

## 13. 尚未解决的问题

1.
2.

如果没有：

无。

---

## 14. 当前阻塞

无 / 具体说明

---

## 15. progress.md 更新

本次新增完成项：

* [x]

仍未完成：

* [ ]

---

## 16. 是否建议进入下一步

YES / NO

原因：

---

## 17. 最终验收结论

PASS / FAIL

如果 FAIL：

必须先修复：

1.
2.

````

---

# 18. 完成后的对话输出

完成开发以后，在对话中不要只说：

> 已完成。

应该简要输出：

```text
本次任务已完成。

实际验证：
- 编译：PASS
- 测试：PASS
- 启动：PASS
- 核心链路：PASS / N/A

已更新：
docs/progress.md
docs/acceptance-report.md

请对 acceptance-report.md 进行验收。
````

---

# 19. 不允许伪造验收

如果由于环境原因无法验证：

```text
MySQL 不可用
微信开发工具不可用
缺少环境变量
外部 API 不可用
```
[pom.xml](pom.xml)
必须明确写：

```text
NOT VERIFIED
```

不能写：

```text
PASS
```

不能根据“代码看起来正确”推断实际运行通过。

---

# 20. 当前最高原则

开发过程中始终优先：

```text
真实可运行
>
主闭环正确
>
数据可靠
>
可维护
>
自动化
>
代码形式上的复杂和漂亮
```

## 小程序 UI 设计约束

小程序当前确认的视觉基准：

docs/design/miniapp-ui-reference.png

开发首页、学习、能力以及后续学习页面时，
必须以该图片作为主要视觉参考。

优先级：

视觉参考图
>
产品阶段指令
>
当前旧页面样式

旧页面只保留业务逻辑，不作为视觉设计参考。

除非用户明确要求重新设计，否则不得自行改变：

- 蓝色 + 青绿色品牌方向
- 浅灰蓝背景
- 白色圆角卡片
- 清爽、现代、轻量的视觉风格
- 首页 / 学习 / 能力三个底部 Tab
- 当前信息层级和核心页面结构

UI 开发完成不能仅凭代码判定 PASS。

必须：
微信开发者工具实际运行
→ 截图
→ 与参考图比较
→ 明显仍像 Demo 时继续调整。


最终目标不是：

> 写很多代码。

而是：

> 让 gk-study-helper 真正知道用户当前的问题，并合理决定下一步学习。
